import 'package:flutter/material.dart';

class DaurUlang extends StatelessWidget {
  const DaurUlang({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow,
      ),
      body: const Center(
        child: Text('Mantap men'),
      ),
    );
  }
}